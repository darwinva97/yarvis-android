apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: yarvis

resources:
  - namespace.yaml
  - configmap.yaml
  # - secret.yaml  # Excluido: los secretos se manejan en repo privado
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: yarvis
  app.kubernetes.io/version: "1.2.0"
  app.kubernetes.io/managed-by: argocd

# Para diferentes ambientes, crear overlays:
# k8s/overlays/dev/kustomization.yaml
# k8s/overlays/prod/kustomization.yaml
